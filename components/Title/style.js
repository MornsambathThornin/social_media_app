import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  title: {
    fontFamily: 'Inter',
    fontWeight: 'bold',
    fontSize: 28,
    lineHeight: 36,
  },
});

export default style;
